# Ansible config for Ignition config generation (run from WSL)
[defaults]
inventory = localhost,
host_key_checking = False
roles_path = roles
# Playbook runs on localhost; no SSH
[privilege_escalation]
become = False
